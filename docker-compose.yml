version: '3.7'
services:
  db:
    image: postgres:15
    restart: always
    ports:
    - "5432:5432"
    env_file:
      - .env
  app:
    build:
      context: .
    depends_on:
      - db
    ports:
      - "8000:8000"
    env_file:
      - .env